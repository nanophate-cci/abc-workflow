version: 2.1

jobs:
  job:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run: |
          # echo Hello, World!

  build:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run: |
          # echo Hello, World!

  test:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run: |
          # echo Hello, World!

workflows:
  C:
    jobs:
      - test
  B:
    jobs:
      - build
  A:
    jobs:
      - build:
          serial-group: deploy-group
      - test:
          serial-group: deploy-group
      - job
