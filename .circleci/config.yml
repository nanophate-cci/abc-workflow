version: 2.1

jobs:
  job:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run: |
          # echo Hello, World!

  test:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run: |
          # echo Hello, World!

workflows:
  C:
    jobs:
      - test
  B:
    jobs:
      - job
  A:
    jobs:
      - job
